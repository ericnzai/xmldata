@using eXml.Models
@model List<SelectListItem>

@Html.DevExpress().ComboBox(
                        settings => {
                        settings.Name = "consultant";
                        settings.Width = 200;
                        settings.SelectedIndex = 0;
                        //settings.Properties.EnableCallbackMode = true;
                        settings.CallbackRouteValues = new { Controller = "InventoryReport", Action = "_GetUnitConsultants" };
                        settings.Properties.ClientSideEvents.BeginCallback = @"function(s, e) { e.customArgs['unit'] = unit.GetValue(); }";
                        settings.Properties.IncrementalFilteringMode = IncrementalFilteringMode.StartsWith;
                        settings.Properties.DropDownStyle = DropDownStyle.DropDownList;
                        //settings.Properties.TextFormatString = "{1} {2}";
                        settings.Properties.TextField = "Text";
                        settings.Properties.ValueField = "Value";
                        settings.Properties.ValueType = typeof(string);
                        settings.Properties.ClientSideEvents.Init = "function(s,e) { consultantInit(s,e); }";
                        //settings.Properties.Columns.Add("EmployeeNo", "EmpNo", 80);
                        //settings.Properties.Columns.Add("FirstName", "FirstName", 100);
                        //settings.Properties.Columns.Add("LastName", "LastName", 100);
                        //settings.Properties.ClientSideEvents.SelectedIndexChanged = "GetUnit";
                        }).BindList(Model).GetHtml()