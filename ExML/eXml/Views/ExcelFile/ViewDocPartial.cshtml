@Html.DevExpress().Spreadsheet(
    settings => {
        settings.Name = "spreadsheet";
        settings.CallbackRouteValues = new { Controller = "ExcelFile", Action = "ViewDocPartial" };
        settings.Width = Unit.Percentage(100);
        settings.Height = 700;
        settings.ActiveTabIndex = 0;
        settings.ShowConfirmOnLosingChanges = false;
        //settings.WorkDirectory = Path.GetDirectoryName;
        //settings.SettingsDocumentSelector.UploadSettings.Enabled = !Utils.IsSiteMode;
    }
).Open(Path.Combine(Server.MapPath("~/Content/Uploads"), "GE OW.xls")).GetHtml()